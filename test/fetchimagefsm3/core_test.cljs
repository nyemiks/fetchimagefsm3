(ns fetchimagefsm3.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [fetchimagefsm3.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
